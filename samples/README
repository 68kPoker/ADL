This directory contains the following subdirectories:

	aard			-- A complete ADL specification
	demos			-- Demonstrations of specific ADL features.
	mpu			-- A sample ADL game originally used to
				   test ADL features.

It also contains the file "standard.adl" which contains elegant
and useful routines to help standardize ADL scenarios.  It is
used by both aard and mpu.

To make them, simply type "make" in this directory.
